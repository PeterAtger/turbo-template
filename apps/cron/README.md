# EC2 Cron Job Setup Deployment Instructions

Follow the steps below to deploy the cronjob.

## 1. Install Dependencies

Run this command to install all required dependencies:

```bash
yarn install
```

## 2. Build the project:

```bash
yarn build
```
